# GlassTrax Agent - Minimal Dependencies
# These are bundled with the standalone installer
# For development, use the main requirements.txt
#
# NOTE: pyodbc is NOT included - it requires:
#   1. C++ build tools to compile
#   2. ODBC drivers installed on target system
# The agent will prompt user to install pyodbc if missing.

# Core API
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.0.0
pydantic-settings>=2.0.0

# Configuration
ruamel.yaml>=0.18.0

# Authentication (has pre-built wheels)
bcrypt>=4.0.0

# System Tray (Windows)
pystray>=0.19.5
Pillow>=10.0.0
